#include<iostream>
#include <numeric>
#include<string>

using namespace std;

int main()
{
	int arr[100001];
	long long  n, sum = 0;
	cin >> n;

	for (int i = 0; i < n; i++)
	
		cin >> arr[i];
		sum = accumulate(arr, arr + n, sum);
		if (sum < 0)
		{
			sum *= -1;
		}
		cout << sum << endl;
}
