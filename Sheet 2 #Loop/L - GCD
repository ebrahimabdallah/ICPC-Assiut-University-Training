#include<iostream>
#include<algorithm>
using namespace std;
int main()
{
 
    int N,S;
    int max;
    cin >> N;
    cin >> S;
  
    for (int i = 1; i <=S &&i<=N; i++) {
 
        if (N % i == 0 && S % i==0)
        {
            max = i;
 
        }        
    }
    cout << max << endl;
}
